import 'package:flutter/material.dart';

Widget buildCmsWebVideoPlayer({required String url}) {
  return const Center(
    child: Text('CMS web player is available on Flutter web only.'),
  );
}

Widget buildHlsWebVideoPlayer({required String url}) {
  return const Center(
    child: Text('HLS player is available on Flutter web only.'),
  );
}
